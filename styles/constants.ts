interface Colors {
    primary: string;
    secondary: string;
    white: string;
    black: string;
    link: string;
}

interface Fonts {
    fonts: string;
}

interface Sizes {
    width: number;
}

export const COLORS: Colors = {
    primary: '#8D99AE',
    secondary: '#2B2D42',
    white: '#FFFFFF',
    black: '#000000',
    link: '#2B2D42',
};

export const FONTS: Fonts = {
    fonts: 'Poppins',
};

export const SIZES: Sizes = {
    width: 327,
};
